Combinand is still using the old Generate dynamics module. need to set up module initializes to do that.
but since Combinand itself is explicitly created, it will need to have @operator set dynamically at some point during initialize.
how? ancestor chain introspection probably needed